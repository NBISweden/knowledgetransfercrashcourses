# List the channels where the requested conda packages reside
# Here, we needed to add `conda-forge` as well as there were an
# implicit hidden dependency on that -- not common!
channels:
  - conda-forge
  - bioconda
# List the packages needed by the calling snakemake rule
dependencies:
  - bowtie =1.1.2
